<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="tech.redroma.yoching">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_round"
            android:label="@string/app_name"
            android:logo="@mipmap/ic_launcher_round"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/YoChingTheme">
        <activity android:name=".activities.YoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
                android:name=".activities.ReadActivity"
                android:exported="true"
                android:label="@string/title_activity_read_wrexagram"
                android:theme="@style/YoChingTheme">
        </activity>
        <activity android:name=".activities.CreditsActivity">
        </activity>
    </application>

</manifest>